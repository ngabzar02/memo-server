# R2 — Round 2 (MCP langsung via daemon HTTP)

- **Tanggal**: 2026-08-03
- **Runner**: user (19 pasang query: 8 Blok A sisa + 11 Blok B) via daemon :4041
- **Skor**: resolve 33/35 (94%, 19/19 kandidat; 18x trust 95, sqlite-vec 2.9)
  docs hit@1 2/14 (14%), hit@5 3/14 (21%) — target 40% BELUM
- **Catatan**: 8 query BRUTAL beda teks dari queries.json → tak terskor score.py

## Akar masalah (5, oleh Agent B)
1. 5 lib top kosong/nyaris: sqlalchemy, litestar, openai, click (0 hasil),
   polars (3 tapi path salah) — ingest parsial (deadline 20s) / gagal
2. Korpus 1-path: express, prisma, tailwindcss, django (llms-full.txt
   tunggal) → top-5 duplikat path sama
3. docs_url salah: fastmcp → glama.ai, anthropic → pkg.go.dev,
   sqlite-vec → GitHub README (trust 2.9)
4. 8 query BRUTAL ≠ queries.json → score.py exact-match tidak hit
5. resolve 33/35: 2 miss (tak tercatat) — review

## Perbaikan (O, langsung)
- aliases.json: fastmcp → gofastmcp.com (jlowin.github.io 404!), tambah
  sqlite-vec → alexgarcia.xyz/sqlite-vec trust 95 [VERIFIED: curl HEAD 404/308]
- Daemon memo di-restart utk muat alias baru
- Reingest 9 lib bermasalah: 3x get_docs per lib (iterative deepening, FTS
  deadline 20s/call) — log /tmp/opencode/reingest.log

## Verifikasi awal resolve (via daemon, setelah fix)
- fastmcp → gofastmcp.com t95 ✓, anthropic → docs.anthropic.com t95 ✓,
  sqlalchemy → docs.sqlalchemy.org t95 ✓, sqlite-vec → alias baru ✓

## Next
- Verifikasi chunk count hasil reingest → re-bench subset (user) → R3

## Perbaikan lanjutan (O, setelah analisa)
- AKAR baru: probe llms-full.txt dapat 404-page dgn status 200 (Sphinx) →
  1 chunk sampah ("Can't find the page"). Fix `_looks_404` di ingest.py —
  teruji sqlalchemy 1 → 276 chunk, 7 path [VERIFIED: debug langsung 2026-08-03]
- AKAR baru: flag `full=1` dari korpus era bug (1-path) → get_docs TIDAK
  pernah reingest → warmup --force utk 6 lib (sqlalchemy litestar
  tailwindcss django express prisma), log /tmp/opencode/warmup3.log
- `_REQUEST_BUDGET` 20 → 30s (daemon+bridge izinkan; ingest lebih dalam/call)
- express guides-5x.txt: 201 chunk @1s — korpus akan jauh lebih kaya setelah
  warmup (4 file llms, cap 300 chunk/lib)

## Verifikasi DB awal (sebelum warmup3)
- openai 29 path, click 76 path, polars 37 path — sehat
- sqlalchemy/litestar/tailwindcss/django/express/prisma: 1 path (sampah/era bug)

## Perbaikan lanjutan 2 (O)
- Bug warmup CLI: sintaks `memo warmup` (tanpa dash) tak dikenali → jalur mcp.run()
  (warmup2 sia-sia). Benar: `memo --warmup --force <lib>`.
- Bug budget warmup: `deadline=None` ternyata 60s keras (komentar bohong) →
  `float("inf")`; teruji langsung: sqlalchemy 205 chunk 30 path @35s
  [VERIFIED: python debug 2026-08-03 01:37]
- Race: 2x warmup3 paralel (lupa pkill) → hasil tak konsisten → pkill + single.
- DAEMON restart: kode 404-filter aktif utk MCP path (get_docs).
- Status DB (01:47): sqlalchemy 30 chunk 30 path (sehat, bukan sampah);
  litestar/tailwindcss/django/express/prisma dalam proses warmup.

## Kesimpulan round
- Target hit@5 40%: korpus hampir semua lib kini multi-path — peluang besar
  naik. Menunggu re-bench user (R3).

## Akar masalah ditemukan (02:47) — add_chunks self-delete
- `store.add_chunks` menghapus chunk lama per-path DI DALAM loop insert:
  untuk file llms (1 file = 100+ chunk, path sama) setiap insert menghapus
  chunk sebelumnya → korpus = 1 chunk/path. Ini AKAR "CLI warmup 30 vs
  langsung 205", express 1 chunk, dan semua lib 1-path sejak R1.
- Fix: hapus path lama SEKALI di awal, lalu insert semua chunk.
  Verified: 200 chunk express → 200 tersimpan (sebelumnya 1).
  [VERIFIED: python test 2026-08-03 02:47]
- Fix pendukung: probe llms.txt WAJIB plain text (404-page HTML ber-status
  200 dari Sphinx/RTD/GitHub Pages — litestar root 404, django llms-full
  53KB HTML, express llms.txt pindah ke root).
- Alias: litestar -> docs.litestar.dev/latest/ (root 404), express ->
  expressjs.com (root; /en/llms.txt 404).
- Commit fa4caee. Warmup ulang: express sqlalchemy litestar django.
