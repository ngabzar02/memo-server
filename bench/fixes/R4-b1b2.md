# R4 fixes: b1 (trim_to_tokens kosong) & b2 (chunk raksasa)

## BUG 1 — `src/memo/store.py:190`
`break` -> `continue`: chunk oversize di posisi top-1 kini di-skip, hasil lain tetap
dikirim (sebelumnya return `[]` ke client MCP). `_demo()` baris 213 di-update:
assert lama mengukuhkan perilaku salah (`== []`), kini membuktikan
`trim_to_tokens([{"text":"x"*20000},{"text":"ok"}]) == [{"text":"ok"}]`.

## BUG 2 — `src/memo/ingest.py:76-90` + helper `_split_oversize` (~baris 91)
Paragraf tunggal `pt > max_tokens` kini dipecah jadi potongan `<= max_tokens*4`
char: cari newline terdekat sebelum batas (`rfind`), tak ada -> potong mentah.
`cur2` di-flush dulu agar tidak ada chunk > budget. ponytail: hard split per
karakter, code block masih bisa terpotong (upgrade ke parse fence bila perlu).

## Sabotase (pre-fix -> post-fix)
```
BUG1 pre:  store.trim_to_tokens([{'text':'x'*20000},{'text':'kecil'}]) -> []
BUG2 pre:  chunk_text('A'*100000) -> 1 chunk, max_len=100000
BUG1 post: -> [{'text':'kecil'}]                                     [VERIFIED]
BUG2 post: 98 chunk, max_len=1024 (<=4096); max_tokens=256 dihormati   [VERIFIED]
newline guard: 36 chunk, max_len=1019, potong di newline OK            [VERIFIED]
```
`python -m memo.store` & `python -m memo.ingest` -> SELFCHECK PASS keduanya.
Catatan: test newline 60k baris lambat — hang O(n²) pre-existing di ingest.py:57
(`in_code` dihitung per baris), di luar scope fix ini.
