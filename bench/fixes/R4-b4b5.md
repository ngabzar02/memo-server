# R4 Bug 4+5 — Fix Report

## BUG 4: crawler tanpa filter domain/bahasa
- Fix: `src/memo/ingest.py:20-25` `_path_allowed(url, base_url)` (netloc sama + skip segmen bahasa non-EN); dipakai di `_crawl` `src/memo/ingest.py:196` menggantikan `startswith(base)`.
- Sabotase sebelum: `web.dev/articles` lolos (netloc beda); `/pt-br/6.0/` lolos → korpus nextjs 101 chunk web.dev, django 200 chunk 10 bahasa.
- Sesudah: `nextjs.org/docs/app/page`→allowed; `web.dev/articles`→NOT; `docs.djangoproject.com/pt-br/6.0/intro/`→NOT; `/en/6.0/ref/`→allowed.

## BUG 5: full flag palsu
- Fix: `src/memo/ingest.py:27-30` `is_full(complete, n_chunks)` (perlu ≥3 chunk); dipakai di `src/memo/server.py:161,176` (2 titik UPDATE `full=?`).
- Sabotase sebelum: requests 1 chunk `full=1` → re-ingest tidak pernah terjadi.
- Sesudah: `is_full(True,1)`→False (full=0 → re-ingest di call berikutnya, diinginkan); `is_full(True,5)`→True; `is_full(False,5)`→False.

## Bukti
`/root/.local/share/uv/tools/memo/bin/python src/memo/ingest.py` → `SELFCHECK ingest: PASS` (assert B4/B5 di `_demo`, src/memo/ingest.py:272-282); run ad-hoc 7 assert → `ALL PASS`.

## Catatan (bukan bagian fix)
`src/memo/aliases.json:154` requests → `docs_url` = `.../user/advanced/` (halaman spesifik) — isu registry terpisah; dengan fix B5 requests akan re-ingest berulang sampai alias diperbaiki.
