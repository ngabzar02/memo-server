# R4 — Fix B3+B6 (agent F)

## BUG 3 — invalidasi docs_url non-GitHub
- `src/memo/server.py:144` — gate `"github.com" in docs_url` DIBUANG; `_docs_changed` dipanggil selalu.
- `src/memo/server.py:42-43,203-225` — cache `_docs_changed_cache {lib: monotonic}`, TTL 3600s; fresh → skip `registry.resolve` (network). Drop tetap `drop_lib` + return True.
- Sebelum: fastmcp DB `glama.ai` padahal resolve `gofastmcp.com` — chunk basi permanen (R4 §3).
- Sesudah (sabotase, DB temp + monkeypatch resolve): docs_url beda non-github → `drop_lib` + 0 chunk; panggilan ke-2 `_docs_changed` → 0 panggilan resolve (counter).

## BUG 6 — latest_ver/versions kosong di resolve
- Akar: `src/memo/registry.py:449-459` — entri npm bare (repo+docs_url "") dibuang di dedupe, info versi lenyap (tsup 8.5.1). Fix: versi di-merge dulu ke kandidat id sama sebelum filter.
- `src/memo/server.py:96-106` — fallback `store.get_lib` (tanpa network) utk cands versi-kosong.
- Sebelum: `server.resolve_library_id('tsup')` → `latest_ver:"" versions:[]` (terbukti pre-fix).
- Sesudah: tsup → `8.5.1` + 20 versi; flask (alias curated) → `3.1.3` via DB fallback.

## Pembuktian
- `py_compile src/memo/*.py` → OK; `python src/memo/registry.py` → SELFCHECK PASS.
- Bug3: `_docs_changed` drop+cache → PASS; Bug6: resolve_library_id tsup/flask → PASS (blok python di shell).
- Catatan: `ingest.py:160` sempat SyntaxError (2 statement 1 baris) — sudah bersih sebelum edit; dipakai sebagaimana adanya.
